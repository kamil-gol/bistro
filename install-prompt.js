/**
 * PWA Install Prompt Manager for Bistro Pętla
 * Version: 1.0.1
 * DISABLED per user request - keeping file for potential future use
 */

// PWA Install functionality has been disabled
// The PWA will still work, but install prompts are hidden

console.log('📱 PWA Install Prompt is disabled');

// Keep minimal functionality for potential future re-enablement
class PWAInstallPrompt {
    constructor() {
        // Disabled - do not initialize
        console.log('ℹ️ PWA Install UI is disabled');
    }
}

// Do not initialize
// window.pwaInstallPrompt = new PWAInstallPrompt();